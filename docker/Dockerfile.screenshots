FROM mcr.microsoft.com/playwright:v1.60.0-noble

WORKDIR /app

# playwright (browser already bundled in image) + sharp for PNG→WebP re-encoding
RUN npm init -y && npm install playwright sharp

COPY scripts/screenshots.mjs ./scripts/screenshots.mjs
